name: Tests

on:
  push:
    branches:
      - lab1
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
          dotnet-quality: 'preview'

      - name: Restore dependencies
        run: dotnet restore VetClinic/VetClinic.slnx

      - name: Build
        run: dotnet build VetClinic/VetClinic.slnx --no-restore --configuration Release

      - name: Test
        run: dotnet test VetClinic/VetClinic.slnx --no-build --configuration Release --verbosity normal